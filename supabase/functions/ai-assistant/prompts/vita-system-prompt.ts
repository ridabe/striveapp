// Arquivo substituído — o assistente agora se chama Max Strive.
// Este arquivo existe apenas para compatibilidade; importe de max-system-prompt.ts.
export { buildMaxSystemPrompt as buildVitaSystemPrompt } from './max-system-prompt.ts';
